{
  "name": "ubavoy-tests",
  "private": true,
  "type": "module",
  "description": "Pruebas de seguridad de las reglas de Firestore de UbaVoy",
  "scripts": {
    "test": "cd .. && firebase emulators:exec --only firestore --project demo-ubavoy \"node tests/firestore-rules.test.mjs\""
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
